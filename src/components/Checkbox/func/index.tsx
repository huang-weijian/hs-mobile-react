/**
 * 根据check状态返回不同的icon
 * return different icon node by checkbox state
 */
import {ICheckboxProps} from "../Checkbox";
import {ReactNode} from "react";

export function getCheckStateIcon(props:ICheckboxProps):{checked:ReactNode,notChecked:ReactNode}{

}
